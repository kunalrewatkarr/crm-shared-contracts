name: Deploy
on:
  workflow_dispatch:
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Publish @nexhub/contracts to GitHub Packages when ready."
