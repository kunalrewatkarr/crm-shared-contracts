# @nexhub/contracts

Shared TypeScript types, enums, Zod schemas, and permission names for NexHub.

```bash
npm install
npm run build
```

Consumers: `crm-client-api`, `crm-superadmin-api`, `crm-client-web`, `crm-superadmin-web`.
